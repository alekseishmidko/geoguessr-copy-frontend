import React, { FC } from "react";

export const RestartScreen: FC = () => {
  return (
    <div className="absolute inset-0 flex items-center justify-center bg-black/60 text-white text-xl font-bold">
      Click to Restart
    </div>
  );
};
